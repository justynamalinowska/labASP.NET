@model Producent?
@{
}

<h1>Details for product: @Model.Name </h1>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Name): @Model.Name</th>
            <th>@Html.DisplayNameFor(model => model.Description): @Model.Description</th>
            <th>@Html.DisplayNameFor(model => model.Country): @Model.Country</th>
        </tr>
    </thead>
</table>
    <div>
        <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
